.breadcrumb
    &__home .breadcrumb__link:before
        display: none

    &__link
        color: var(--color-text)
        font-weight: regular
        font-size: 14px

    &__last
        color: var(--primary)
        font-weight: bold
        font-size: 14px

    &__sep
        .ico
            width: 5px
            height: 10px
            color: var(--color-text)